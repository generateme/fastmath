(ns vector-matrix
  (:require [fastmath.vector :as v]
            [fastmath.matrix :as mat]
            [fastmath.dev.codox :as codox]))

;; # Vectors and matrices {.unnumbered}

;; ## Vectors

;; ### Reference

(codox/make-public-fns-table-clay 'fastmath.vector {:level "####"})

;; ## Matrices {.unnumbered}

;; ### Reference

(codox/make-public-fns-table-clay 'fastmath.matrix {:level "####"})
