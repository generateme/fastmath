(ns ml
  (:require [fastmath.ml.regression :as regr]
            [fastmath.ml.clustering :as clust]
            [fastmath.dev.codox :as codox]))

;; # Machine Learning {.unnumbered}

;; ## Regression

;; ## Clustering

(codox/make-public-fns-table-clay 'fastmath.ml.regression)
(codox/make-public-fns-table-clay 'fastmath.ml.clustering)

